import 'package:flutter/material.dart';

class AnimalFactScreen extends StatelessWidget {
  const AnimalFactScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(),
    );
  }
}
